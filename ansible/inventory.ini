# Corré el provisioning contra el VPS.
#
# Opción A — desde el propio VPS (lo más simple; el orquestador corre ahí):
[wisnee]
localhost ansible_connection=local

# Opción B — remoto (descomentá y ajustá; comentá la línea de arriba):
# wisnee-vps ansible_host=TU_IP_PUBLICA ansible_user=root
