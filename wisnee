#!/usr/bin/env python3
"""Entrypoint del orquestador. Uso:

    sudo ./wisnee init
    ./wisnee update | status | logs [svc] | seed | backup | cert | credentials

Corré desde la raíz del repo (donde vive este archivo)."""

from orchestrator.cli import main

if __name__ == "__main__":
    main()
